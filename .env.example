# Unified Environment Variables Example
# Copy this to .env in each package or use a global one if supported by your deployment

# --- Supabase ---
SUPABASE_URL=your-project-url.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# --- OpenAI ---
OPENAI_API_KEY=sk-....

# --- Nutrition APIs ---
USDA_API_KEY=your-usda-api-key
# Open Food Facts doesn't usually require a key for basic usage, but add if needed
OFF_API_KEY=your-off-api-key

# --- Frontend ---
NEXT_PUBLIC_SUPABASE_URL=${SUPABASE_URL}
NEXT_PUBLIC_SUPABASE_ANON_KEY=${SUPABASE_ANON_KEY}

# --- Shared ---
# Add any cross-package constants here
NODE_ENV=development
