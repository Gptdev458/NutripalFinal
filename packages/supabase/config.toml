# Supabase Edge Functions Configuration
# Note: ai-handler and ai-handler-v2 have been removed during the rehaul
# The nutrition-lookup module is a library, not a deployable function

# Add new edge function configurations here as they are developed
